/* pnt_add.c -- 指针加法 */
#include <stdio.h>
#define SIZE 4

int main(int argc, char const *argv[])
{
	short dates[SIZE];
	short * pti;
	short index;
	double bills[SIZE];
	double * ptf;

	pti = date; // 把数组地址赋给指针
	ptf = bills;
	return 0;
}
